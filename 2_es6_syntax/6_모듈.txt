이전의 js에서는 <script></script>
태그의 호출 순서로 의존성을 관리함

script에서 전역변수 공간을 사용해서 (window.)
기능들을 모듈별로 구현해서 공유함

--------------------------------------

import를 이용해서 script 엘리먼트를 쓰지 말고
연결된 파일들을 먼저 다운받고 여기서 필요한 
내용물만 얻어옴

as를 써서 이름을 바꿔서 임포트 가능