비동기 처리
- 시간이 많이 걸리는 작업을 처리하느라
- 다른 작업이 블로킹 되지않게 실행을 다음
- 라인으로 넘기는 것

api 호출하고 기다리는 것이 대표적이다.

기존의 js는 콜백을 기반으로 비동기를 처리
함수의 계단 형태가 형성되어 콜백 지옥이라 칭함

-----------------------------------

Promise의 등장
- 미래에 완료될 작업에 대한 추상화

Promise(resolve함수, reject함수)

프로미스는 연결되는 사슬과 같다.
then에서 리턴하는 값은 프로미스다.
일반 값 리턴시 promise로 값을 감싸서 리턴해준다.
